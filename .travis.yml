language: node_js

node_js:
  - 10

sudo: true

addons:
  chrome: stable

cache:
  directories:
    - node_modules

install:
  - npm ci

script:
  - npm run lint ngx-container
  - npm run test ngx-container -- --watch false --browsers ChromeHeadless
